[project]
name = "betty-voice"
version = "0.1.0"
description = "External voice assistant for VTOL VR"
requires-python = ">=3.10"

[project.scripts]
betty-voice = "betty_voice.main:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
